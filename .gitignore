.env
superset-metadata-backup-*.sql
.venv/
logs/
__pycache__/
*.pyc 
.pytest_cache/
dbt/opensky/target/
dbt/opensky/dbt_packages/
dbt/opensky/.user.yml

# Garage config carries RPC secret + admin tokens
config/garage.toml

# Host-specific compose overrides (e.g. LAN port exposure); loaded via COMPOSE_FILE
docker-compose.local.yml
